export const environment = {
  production: true,
  apiUrl: 'https://your-domain.com/api',
  // Public PayPal client ID — safe to expose on the frontend. The secret stays
  // server-side only. Replace with your live client ID.
  paypalClientId: 'YOUR_PAYPAL_LIVE_CLIENT_ID',
};
